[package]
name = "krkr2-bridge"
version = "0.1.0"
edition = "2021"
description = "CXX bridge bindings for KrKr2 C++ engine types (tTJSBinaryStream, ttstr)"

[lib]
crate-type = ["staticlib"]

[dependencies]
cxx = "1.0"

[build-dependencies]
cxx-build = "1.0"
